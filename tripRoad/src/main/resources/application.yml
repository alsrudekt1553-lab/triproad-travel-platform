server:
  port: 8587
# Oracle Connect
spring:
  application:
    name: tripRoad
    
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

  #Datasource Bean 생성
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@172.30.1.16:1521/xe
    username: tripRoad
    password: tiger

  # View Resolver -->  X

  #Jpa Setting
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none


logging.level:
  org.hibernate.SQL: debug   # logger를 통해 하이버네이트 실행 SQL      
  
# Mybatis
mybatis:
  config-location: classpath:configuration.xml
  mapper-locations: classpath:mappers/**/*.xml # mappers 아래 모든 하위폴더의 mapper 허용

#upload Folder
com.oracle.tripRoad.upload.path: upload

# Mail (SMTP)
spring.mail:
  host: smtp.gmail.com
  port: 587
  username: tripRoad999@gmail.com
  password: 'veon plvb jahw nrqk'
  properties:
    mail.smtp.auth: true
    mail.smtp.starttls.enable: true

# KakaoPay
kakaopay:
  secret-key: DEV_SECRET_KEY_본인키
  cid: TC0ONETIME
  ready-url: https://open-api.kakaopay.com/online/v1/payment/ready
  approve-url: https://open-api.kakaopay.com/online/v1/payment/approve